{
  "name": "typescript-vite-lit",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "bash -c 'prettier --ignore-path <(cat .gitignore .git/info/exclude) --write .'",
    "check": "tsc --watch"
  },
  "dependencies": {
    "lit": "~3"
  },
  "devDependencies": {
    "prettier": "~3",
    "typescript": "~5",
    "vite": "~5"
  }
}
